#pragma once

#include "AObject.hpp"

class Camera : public AObject {
public:

  Camera(short, short, short, double, double, double);

private:
  // short		x;
  // short		y;
  // short		z;
  // double	rx;
  // double	ry;
  // double	rz;
};
